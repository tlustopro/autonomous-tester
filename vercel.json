{
  "buildCommand": "pip install -r requirements.txt && PLAYWRIGHT_BROWSERS_PATH=./playwright-browsers python -m playwright install chromium",
  "env": {
    "PLAYWRIGHT_BROWSERS_PATH": "./playwright-browsers"
  }
}
